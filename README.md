# Aplicativo desenvolvido utilizando ionic2 (angular2, less, typescript)

Para executar o aplicativo execute os comandos:

* npm install
* ionic serve ou ionic emulate ios --target="iPhone-4s, 9.3" --l
